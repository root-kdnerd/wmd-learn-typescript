function getRectangleArea(width: number, height: number) {
    return width * height;
  }
  
  function getCircleArea(radius: number) {
    return Math.PI * radius * radius;
  }
  
  module.exports = { getRectangleArea, getCircleArea };

  export{};
  

