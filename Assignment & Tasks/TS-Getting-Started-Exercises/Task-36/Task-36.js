function make_shirt(size, message) {
    console.log("The shirt size is ".concat(size, " and the message on the shirt is \"").concat(message, "\"."));
}
make_shirt("XL", "I love TypeScript!"); // Call the function
